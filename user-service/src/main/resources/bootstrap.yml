server:
  port: 9091
spring:
  application:
    name: USER-SERVICE
  cloud:   
    config:
      enabled: true
      uri: http://localhost:9095
  datasource:
    url: jdbc:mysql://localhost:3306/hoteldb1
    username: root
    password: Yappi@2017
  jpa:
    hibernate:
      ddl-auto: update  
    
logging:
  level:
    root: info
    
