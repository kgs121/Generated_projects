server:
  port: 9092
spring:
  application:
    name: PAYMENT-SERVICE
  cloud:   
    config:
      enabled: true
      uri: http://localhost:9095
  datasource:
    url: jdbc:mysql://localhost:3306/hoteldb1
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update  
    
logging:
  level:
    root: info
    
